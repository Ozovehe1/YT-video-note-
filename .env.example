# Get an API key at https://platform.claude.com/
ANTHROPIC_API_KEY=sk-ant-...

# Optional: proxy for YouTube transcript requests (needed on most cloud hosts,
# where YouTube blocks datacenter IPs). Not needed when running locally.
# YT_PROXY=http://user:pass@host:port
