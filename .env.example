# ----- LLM (note generation) — DeepSeek v4 Pro via NVIDIA -----
# Get a key at https://build.nvidia.com (OpenAI-compatible endpoint).
NVIDIA_API_KEY=nvapi-...
# Model + endpoint (both overridable). Defaults target NVIDIA's DeepSeek v4 Pro.
LLM_MODEL=deepseek-ai/deepseek-v4-pro
LLM_BASE_URL=https://integrate.api.nvidia.com/v1
# Classification (monologue vs dialogue) — defaults to the same model. Override
# only if you want a cheaper/faster model for it.
LLM_CLASSIFY_MODEL=deepseek-ai/deepseek-v4-pro

# Requests-per-minute the note generator is allowed to make. We pace under the
# provider's per-minute cap and back off on 429s. Keep it modest if you open the
# app on multiple devices at once (each device generates independently).
NEXT_PUBLIC_LLM_RPM=10

# ---- Supabase (accounts, history, reading progress) ----
# Project Settings → API. Use the URL and the *publishable* (anon) key.
# No service-role/secret key is needed — Row-Level Security protects all data.
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=eyJ...

# ---- Supadata (YouTube transcripts) ----
# https://supadata.ai — works from Vercel's IPs, unlike scraping YouTube directly.
SUPADATA_API_KEY=sd_...

# ---- YouTube Data API (search by title) ----
# https://console.cloud.google.com → enable "YouTube Data API v3" → create an API key.
YOUTUBE_API_KEY=AIza...
